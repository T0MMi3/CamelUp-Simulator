Command to start FastAPI server for AI sugggestion (do first):
 - for terminal/powershell:
    uvicorn ai_server:app --reload
 - for bash
    python -m uvicorn ai_server:app --reload


Command to run simulator:
 dotnet run

